# Mastermind — Multi-AI Handoff Bundle

> **Instructions for the User:** Copy the entirety of this file and paste it into the new AI (ChatGPT, Claude, Gemini, etc.) as your first message to perfectly onboard them into the current state of the project.

---

## INITIALIZATION PROTOCOL

You are receiving a project handoff governed by the **Mastermind Project Governance Protocol v3.0**. 

Before you take any action, you must:
1. Parse the CONFIG BLOCK below to understand your behavioral constraints.
2. Read the `STATE.json` snapshot to understand exactly where the project stands.
3. Review the "Immediate Next Steps" and "Critical Context."
4. Respond with the "Boot Sequence Acknowledgment" (confirming Mastermind v3.0 is active, listing the dial values, and stating readiness).

---

### CONFIG BLOCK
```
RESOURCE_GATE        = [INSERT_VALUE]
ROLLBACK_GATE        = [INSERT_VALUE]
SESSION_BUDGET       = [INSERT_VALUE]
HANDOFF_MODE         = [INSERT_VALUE]
QC_INTENSITY         = [INSERT_VALUE]
CONFIRMATION_MODE    = [INSERT_VALUE]
FOLDER_ENFORCEMENT   = [INSERT_VALUE]
NAMING_STRICTNESS    = [INSERT_VALUE]
STATE_SYNC           = [INSERT_VALUE]
VERBOSITY            = [INSERT_VALUE]
NDA_GATE             = [INSERT_VALUE]
```

*(Note to AI: You must abide by these settings as defined in the Mastermind system prompt.)*

---

### CURRENT STATE SNAPSHOT (`STATE.json`)
```json
[INSERT_STATE_JSON_CONTENTS_HERE]
```

---

### IMMEDIATE NEXT STEPS
1. [Insert Step 1]
2. [Insert Step 2]
3. [Insert Step 3]

---

### CRITICAL CONTEXT & LOGIC TOMBSTONES
- **Unresolved Bugs:** [List any active issues]
- **Logic Tombstones (What we tried that failed):** [List failed approaches to prevent the new AI from repeating mistakes]
- **Key Decisions:** [List any architectural or design decisions that must not be changed]

---

**[Awaiting Boot Sequence Acknowledgment...]**
